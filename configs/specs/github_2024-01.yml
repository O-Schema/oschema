source: github
version: "2024-01"
type_header: "X-GitHub-Event"
type_mapping:
  push: repo.push
  pull_request: repo.pull_request
  issues: repo.issue
  release: repo.release
  star: repo.star
  create: repo.branch_created
  delete: repo.branch_deleted
  fork: repo.forked
  ping: system.ping
  issue_comment: repo.issue_comment
  pull_request_review: repo.pr_review
  pull_request_review_comment: repo.pr_review_comment
  workflow_run: ci.workflow_run
  check_run: ci.check_run
  deployment: deploy.created
  deployment_status: deploy.status
  status: repo.commit_status
fields:
  external_id: ""
  timestamp: ""
  data:
    action: action
    repository: repository.full_name
    repo_private: repository.private
    repo_default_branch: repository.default_branch
    repo_url: repository.html_url
    sender: sender.login
    sender_id: sender.id
    sender_type: sender.type
    ref: ref
    before: before
    after: after
    created: created
    deleted: deleted
    forced: forced
    compare: compare
    number: number
    pr_title: pull_request.title
    pr_body: pull_request.body
    pr_state: pull_request.state
    pr_merged: pull_request.merged
    pr_draft: pull_request.draft
    pr_user: pull_request.user.login
    pr_head_ref: pull_request.head.ref
    pr_head_sha: pull_request.head.sha
    pr_base_ref: pull_request.base.ref
    pr_base_sha: pull_request.base.sha
    pr_html_url: pull_request.html_url
    pr_commits: pull_request.commits
    pr_additions: pull_request.additions
    pr_deletions: pull_request.deletions
    pr_changed_files: pull_request.changed_files
    pr_mergeable: pull_request.mergeable
    pr_merged_by: pull_request.merged_by.login
    pr_created_at: pull_request.created_at
    pr_updated_at: pull_request.updated_at
    pr_closed_at: pull_request.closed_at
    pr_merged_at: pull_request.merged_at
    issue_title: issue.title
    issue_body: issue.body
    issue_state: issue.state
    issue_state_reason: issue.state_reason
    issue_number: issue.number
    issue_user: issue.user.login
    issue_html_url: issue.html_url
    issue_created_at: issue.created_at
    issue_updated_at: issue.updated_at
    issue_closed_at: issue.closed_at
    issue_comments: issue.comments
    release_tag: release.tag_name
    release_name: release.name
    release_body: release.body
    release_draft: release.draft
    release_prerelease: release.prerelease
    release_author: release.author.login
    release_html_url: release.html_url
    release_created_at: release.created_at
    release_published_at: release.published_at
    head_commit_id: head_commit.id
    head_commit_message: head_commit.message
    head_commit_timestamp: head_commit.timestamp
    head_commit_author: head_commit.author.name
    head_commit_author_email: head_commit.author.email
    pusher_name: pusher.name
    pusher_email: pusher.email
